# Tilla backend — production dependencies
# Generated for deployment on Render / Railway
# Python 3.11+

# Core
Django>=4.2,<5.0
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
django-cors-headers>=4.2.0

# Production WSGI server
gunicorn>=21.2.0

# PostgreSQL driver (binary = no build deps needed on Render)
psycopg2-binary>=2.9.9

# Database URL parsing (reads DATABASE_URL from Railway/Render/Supabase)
dj-database-url>=2.1.0

# Static file serving without nginx (WhiteNoise)
whitenoise>=6.6.0

# .env loading for local development
python-dotenv>=1.0.0
